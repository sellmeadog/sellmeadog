# etl-extract-scryfall-sets

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test etl-extract-scryfall-sets` to execute the unit tests via [Jest](https://jestjs.io).
